#include <stdio.h>

/**
 * main - program starting point
 *
 * Return: end program
 *
*/

int main(void)
{
	char rev_alph;

	for (rev_alph = 'z'; rev_alph >= 'a'; rev_alpha--)
	{
		putchar(rev_alpha);
	}

	putchar('\n');
	return (0);
}
